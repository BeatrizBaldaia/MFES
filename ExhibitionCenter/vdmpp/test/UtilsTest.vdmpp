class UtilsTest  is subclass of Test

operations

	public testAll: () ==> ()
	testAll() == (
		dcl date_0 : Utils`Date := mk_Utils`Date(2018,12,27);
		dcl date_1 : Utils`Date := mk_Utils`Date(2018,12,28);
		dcl date_2 : Utils`Date := mk_Utils`Date(2020,2,28);
		dcl date_3 : Utils`Date := mk_Utils`Date(2100,2,28);
		
		testGetDatesDifference(date_0, date_1, 1);
	);
	private testGetDatesDifference: Utils`Date * Utils`Date * nat ==> ()
	testGetDatesDifference(d1,d2, n) == (
		assertTrue(Utils`getDatesDifference(d1,d2) = n);
	);
end UtilsTest
