class UtilsTest
operations
private assertTrue: bool ==> ()
 assertTrue(cond) == return
 pre cond;
public static main: () ==> ()
  main() ==
 (
  new UtilsTest().testbefore();
 );
 private testbefore: () ==> ()
 testbefore() ==
 (
 
 -- dcl tet : Utils`Date := mk_Utils`Date(1,1,40); -- TODO dar erro
	dcl date_0 : Utils`Date := mk_Utils`Date(2018,12,27);
 	dcl date_1 : Utils`Date := mk_Utils`Date(2018,12,28);
	dcl date_2 : Utils`Date := mk_Utils`Date(2020,2,27);
 	dcl date_3 : Utils`Date := mk_Utils`Date(2020,11,28);
 	dcl date_4 : Utils`Date := mk_Utils`Date(2019,2,28);
 	dcl date_5 : Utils`Date := mk_Utils`Date(2020,2,29);
  assertTrue(Utils`before(date_0, date_1));
  assertTrue(not Utils`before(date_1, date_0));
  assertTrue(not Utils`before(date_0, date_0));
  assertTrue(Utils`before(date_1, date_2));
  assertTrue(not Utils`before(date_3, date_2));
  assertTrue(not Utils`before(date_5, date_4));
  
 );
end UtilsTest
