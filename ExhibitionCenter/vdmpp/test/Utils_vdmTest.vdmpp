class Utils_vdmTest  is subclass of Test_vdm

operations

	public testAll: () ==> ()
	testAll() == (
		dcl date_0 : Utils_vdm`Date := mk_Utils_vdm`Date(2018,12,27);
		dcl date_1 : Utils_vdm`Date := mk_Utils_vdm`Date(2018,12,28);
		dcl date_2 : Utils_vdm`Date := mk_Utils_vdm`Date(2020,2,28);
		dcl date_3 : Utils_vdm`Date := mk_Utils_vdm`Date(2100,2,28);
		
		testGetDatesDifference(date_0, date_1, 1);
	);
	private testGetDatesDifference: Utils_vdm`Date * Utils_vdm`Date * nat ==> ()
	testGetDatesDifference(d1,d2, n) == (
		assertTrue(Utils_vdm`getDatesDifference(d1,d2) = n);
	);
end Utils_vdmTest
